# Converts an object to a JSON string
json : object

# Returns the default value if the object is null
default : object value:any

# Returns the default value if the object is null or empty
default_if_empty : object value:any

# Returns a boolean indicating whether the object is null
empty : object

# Returns a boolean indicating whether the object is not null
blank : object
